package math;

public class MatrixProblems {
	
	public void rotateMatrixInPlace(int[][] matrix){
		
		//Matrix has to be a square matrix to work
		
		int colNum = matrix.length;
		int rowNum = matrix[0].length;
		
		int maxColIndex = colNum -1;
		int maxRowIndex = rowNum -1;
		
		for(int col = 0; col < colNum; col++){
		}
		

		
	}

}
